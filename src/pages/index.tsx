import type { NextPage } from "next";
import Layout from "../Layout/Layout";
import { Card } from "../Layout/Main/Card";

const Home: NextPage = () => {
  return (
    <Layout>
      <Card />
    </Layout>
  );
};

export default Home;
