import type { NextPage } from "next";
import Layout from "../Layout/Layout";
import { SocialMedia } from "../Layout/Main/SocialMedia";

const SocialMedias: NextPage = () => {
  return (
    <Layout>
      <SocialMedia />
    </Layout>
  );
};

export default SocialMedias;
